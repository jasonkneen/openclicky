# OpenClicky homepage example

A standalone Vite app for the OpenClicky marketing homepage.

```sh
npm install
npm run dev
npm run build
npm run preview
```

The app hydrates GitHub stars, forks, and open issue counts from `jasonkneen/openclicky`, with static fallback values so the page still works offline or when the GitHub API is rate-limited.
